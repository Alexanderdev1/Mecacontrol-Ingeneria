---

import ImgCard from "../ui/imgCard.astro";

const seleccionarImagen = (name:string) => `../../public/imagenes/${name}`;
---

<section class="mx-auto w-[1000px] h-[800px] mt-1 mb-10">
    <h2 class="text-center font-bold text-red-600 text-3xl">Nuestros clientes</h2>
    <p class="text-center mt-12">Estos son unos de nuestros clientes que confiaron en nosotros</p>

    <div class="grid grid-cols-3 gap-6 mt-12">
        <ImgCard imagenUrl={seleccionarImagen("disarko.png")} titulo="Disarko"/>
        <ImgCard imagenUrl={seleccionarImagen("coliseo.png")} titulo="Coliseo Medplus"/>
        <ImgCard imagenUrl={seleccionarImagen("veolia.png")} titulo="Veolia"/>
    </div>
</section>