---
const {titulo,descripcion,imagenUrl} = Astro.props;
---

<section class="bg-red-600 px-10 py-12 mb-24 w-full h-[500px]">
  <div class="max-w-[1300px] mx-auto flex flex-col md:flex-row justify-between items-center h-full">

    <!-- Columna izquierda -->
    <div class="md:w-1/2 mb-8 md:mb-0">
      <h2 class="text-4xl font-bold text-white tracking-tighter mb-4">{titulo}</h2>
      <p class="text-lg text-white leading-relaxed">
       {descripcion}
      </p>
    </div>

    <!-- Columna derecha -->
    <div class="md:w-1/2 flex justify-center">
      <img src={imagenUrl} alt="Cables eléctricos" class="h-[300px] w-[500px] object-cover rounded-2xl shadow-lg">
    </div>

  </div>
</section>
